{% extends 'base.html.twig' %}

{% block body %}
    <h3>Add a serie</h3>
    <div>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Add a program">
        {{ form_end(form) }}
    </div>
{% endblock %}