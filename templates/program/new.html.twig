{% extends 'base.html.twig' %}

{% block body %}
    <h3>Add a serie</h3>
    <div style="padding: 10%">
        {{ form_start(form) }}
        {{ form_row(form) }}
        <input type="submit" value="Add a program">
        {{ form_end(form) }}
    </div>
{% endblock %}